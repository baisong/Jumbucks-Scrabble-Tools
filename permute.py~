#!/usr/bin/python

def permute(list):
    if len(list) <= 1:
        yield list
    else:
        for i in xrange(0,len(list)):
            for tail in permute( list[:i] + list[i+1:] ):
                yield [ list[i] ] + tail

def main():
    for o in permute([1,2,3]):
        print o
